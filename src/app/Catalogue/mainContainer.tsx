export default function MainContainer({ children }: any) {

    return (
        <div className="bg-[#e0e0e0] min-h-screen flex items-center justify-center">
            {children}
        </div>
    )
}